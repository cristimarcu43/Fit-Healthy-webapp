import React from "react";

const Curs = (props) => {
  return (
    <div className="">
      <h2 className="">{props.titlu}</h2>
      <p className="">{props.descriere}</p>
    </div>
  );
};

export default Curs;
